import React from "react";

const Header = () => {
  return (
    <header
      style={{
        background: "#000",
        color: "#fff",
        textAlign: "center",
        padding: "1rem",
      }}
    >
      <h1>→ Content goes here ←</h1>
    </header>
  );
};

export default Header;
